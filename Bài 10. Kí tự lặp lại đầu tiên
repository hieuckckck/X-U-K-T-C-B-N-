#include <cmath>
#include <cstdio>
#include <vector>
#include <bits/stdc++.h>
#include <algorithm>
using namespace std;


int main() {
    string s;
    getline(cin,s);
    map<char,int>mp;
    for(int i=0;i<s.size();i++){
        mp[s[i]]++;
        if(mp[s[i]]==2){
            cout<<s[i];
            return 0;
        }
    }
    cout<<"NONE";
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */   
    return 0;
}
